\subsection{Code}

\subsubsection{Source Code Repository}\label{appendix:source-code}

The source code for \textsc{Natural}, including all pipeline components,
benchmarking infrastructure, and libraries, is available under the AGPL
license:

\begin{center}
\url{https://github.com/mara-schulke/natural}
\end{center}

The repository contains the implementation of all five software components
(\texttt{natural-models}, \texttt{natural-graphs}, \texttt{natural-inference},
\texttt{natural-sampling}, and \texttt{natural-benchmark}) as described in
Section~\ref{section:implementation}.

The WWL kernel bindings using \texttt{pyo3} are maintained in a separate
repository:

\begin{center}
\url{https://github.com/hemisphere-systems/wwl}
\end{center}

\subsubsection{Vector Database API}\label{appendix:vector-db-api}

\begin{verbatim}
let question = "How many ..";

// Initialize vector database and embedding model
let vector = Vector::infer();
let model = EmbeddingModel::from_env().expect("failed to load embedding model");

// Load schema distance index for the target database
let index: DistanceIndex = DistanceIndexRow::get("test_db", &vector)?.into();

// Embed the question and run the multi-dimensional selection algorithm
let selector = Selector::new(question.as_ref(), None, &model)?;
let samples = selection::<8>(&selector, &index, &vector)?;
\end{verbatim}

\paragraph{Vector Database Schema}\label{appendix:vector-schema}

\begin{verbatim}
CREATE TABLE IF NOT EXISTS databases (
  id               text not null,
  name             text not null,
  graph            text not null
);

CREATE TABLE IF NOT EXISTS distance_indices (
  db_id            text not null,
  distances        text not null
);

CREATE VIRTUAL TABLE IF NOT EXISTS samples USING vec0 (
  id                 text not null,
  db_id              text not null,

  question           text not null,
  question_embedding float[4096],

  sql                text not null,
  sql_embedding      float[4096]
);
\end{verbatim}

\paragraph{Benchmark Traits}\label{appendix:benchmark-traits}

\begin{verbatim}
pub trait Benchmark {
    type Error: From<eyre::Report> + Debug;
    type Database: BenchmarkDatabase<Error = Self::Error>;

    fn name() -> &'static str;

    fn databases(&self) -> impl Iterator<Item = &Self::Database>;
    fn database(&self, id: impl AsRef<str>) -> Option<&Self::Database>;

    fn tests(&self) -> impl Iterator<
      Item = &<Self::Database as BenchmarkDatabase>::Test
    >;

    fn test(&self, id: impl AsRef<str>) -> Option<
      &<Self::Database as BenchmarkDatabase>::Test
    >;
}

pub trait BenchmarkDatabase {
    type Error: From<eyre::Report> + Debug;
    type Test: BenchmarkTest;

    fn id(&self) -> &str;
    fn connect(&self) -> Result<Database, Self::Error>;
    fn tests(&self) -> impl Iterator<Item = &Self::Test>;
    fn test(&self, id: impl AsRef<str>) -> Option<&Self::Test>;
}

pub trait BenchmarkTest {
    fn id(&self) -> &str;
    fn db_id(&self) -> &str;

    fn name(&self) -> &str;
    fn question(&self) -> &str;
    fn query(&self) -> &str;
}
\end{verbatim}

\subsubsection{System Configuration}\label{appendix:system-config}

The following table documents all hyperparameters and configuration parameters
used in the \textsc{Natural} system.

\begin{table}[ht]
\centering
\small
\begin{tabular}{lll}
\toprule
\textbf{Component} & \textbf{Parameter} & \textbf{Value} \\
\midrule
\multicolumn{3}{c}{\textbf{Example Selection ($\sigma$)}} \\
\midrule
Similarity Weights & Question semantic similarity & 70\% \\
                   & SQL structural similarity    & 30\% \\
Schema Weighting   & Sample-level similarity      & 70\% \\
                   & Schema-level compatibility   & 30\% \\
Retrieval          & TOP\_K candidates            & 256 \\
                   & Relevance threshold          & 0.5 \\
\midrule
\multicolumn{3}{c}{\textbf{Models}} \\
\midrule
SQL Generation     & Model                        & OmniSQL-7B \\
                   & Quantization                 & GGUF (F16) \\
                   & VRAM usage                   & $\sim$14 GB \\
Embedding          & Model                        & Qwen3-Embedding-4B \\
                   & Quantization                 & Q8\_0 \\
                   & VRAM usage                   & $\sim$2 GB \\
                   & Embedding dimension          & 4096 \\
\midrule
\multicolumn{3}{c}{\textbf{WWL Kernel ($\mathcal{D}$)}} \\
\midrule
Propagation        & Mode                         & Categorical \\
                   & Iterations                   & 3 \\
Distance           & Metric                       & Wasserstein \\
                   & Approximation                & Sinkhorn \\
\bottomrule
\end{tabular}
\caption{Complete system configuration and hyperparameters for \textsc{Natural}.}
\label{tab:system-config}
\end{table}
